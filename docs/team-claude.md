# CLAUDE.md

> Auto-generated by global Claude Code setup. Edit freely for project-specific instructions.
> This file won't be overwritten if it already exists.

## Global Tools Available

This project has access to a globally configured Claude Code environment with **137 agents**, **235 commands**, **127 skills**, and **29 rule files**.

### Core Agents

| Agent | What it does |
|-------|-------------|
| `planner` | Creates implementation plans for complex features |
| `architect` | System design and scalability decisions |
| `tdd-guide` | Enforces test-driven development (write tests first) |
| `code-reviewer` | Reviews code for quality, security, maintainability |
| `security-reviewer` | Detects vulnerabilities (OWASP Top 10) |
| `build-error-resolver` | Fixes build/type errors with minimal diffs |
| `e2e-runner` | Generates and runs Playwright E2E tests |
| `database-reviewer` | PostgreSQL/Supabase query and schema review |

### Key Commands

| Command | Purpose |
|---------|---------|
| `/plan` | Create implementation plan before coding |
| `/tdd` | Test-driven development workflow |
| `/code-review` | Review code quality |
| `/build-fix` | Fix build errors |
| `/commit` | Smart commit with conventional format |
| `/create-pr` | Create pull request |
| `/sparc` | SPARC methodology orchestrator |
| `/verify` | Run full verification loop |

### Workflow Skills (auto-activate)

- **brainstorming** - Structured design before implementation
- **test-driven-development** - RED-GREEN-REFACTOR cycle
- **systematic-debugging** - Root cause before fixes
- **verification-before-completion** - Evidence before claiming done
- **writing-plans** / **executing-plans** - Structured task execution

### GSAP Animation Skills (auto-activate for animation work)

| Skill | Activates when |
|-------|---------------|
| `gsap-core` | Writing tweens, easing, stagger, matchMedia/reduced-motion |
| `gsap-timeline` | Sequencing animations, choreographing keyframes |
| `gsap-scrolltrigger` | Scroll-linked animation, pinning, parallax |
| `gsap-plugins` | Flip, Draggable, SplitText, DrawSVG, MorphSVG, etc. |
| `gsap-utils` | clamp, mapRange, snap, toArray, wrap helpers |
| `gsap-react` | GSAP in React / Next.js (useGSAP hook, cleanup) |
| `gsap-frameworks` | GSAP in Vue / Nuxt / Svelte / SvelteKit |
| `gsap-performance` | 60fps optimization, transforms, quickTo |

### Rules Enforced

- Immutability (never mutate, return new objects)
- 80% minimum test coverage
- TDD workflow (write tests first)
- No hardcoded secrets
- Files < 800 lines, functions < 50 lines

## Project-Specific Instructions

<!-- Add your project-specific Claude Code instructions below -->

